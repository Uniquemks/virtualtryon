import { OutfitCombo } from '../../types/outfit';
import { warmCasual } from './warmCasual';
import { dayOutfit } from './dayOutfit';
import { workday } from './workday';
import { timelessStyle } from './timelessStyle';

export const OUTFIT_COMBOS: OutfitCombo[] = [
  warmCasual,
  dayOutfit,
  workday,
  timelessStyle
];
